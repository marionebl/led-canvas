var Matrix = require('../matrix');

class Text{
	constructor(text) {
		this.chars = [];

		text.split('').forEach((character) => {
			if (character === ' ') {
				this.chars.push
			}

		});
	}

	index(index) {

	}
}

module.exports = Text;
